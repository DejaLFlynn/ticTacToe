const readlineSync = require('readline-sync');
class HumanPlayer{
    constructor(name, sym){
        this.name = name;
        this.sym = sym
    }
    getMove(move){
        return 
    }
}
let game = HumanPlayer(getMove())
console.log(game)